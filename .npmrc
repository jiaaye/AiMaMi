# Relax pnpm supply-chain minimum-release-age policy so install can proceed
# without rejecting recently-published packages (pnpm v10 reads this as a
# kebab-case .npmrc key; the camelCase form is silently ignored).
minimum-release-age=0
